Place fixed TTS clips in this directory:

- `opening.wav`
- `ending.wav`

These files are mounted into `messenger-gateway` at `/app/runtime/tts-fixed` and are concatenated around body-only TTS output.
